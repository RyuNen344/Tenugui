android.buildFeatures.compose = true

composeCompiler {
    metricsDestination.set(file("${layout.buildDirectory.get()}/compose_compiler"))
    reportsDestination.set(file("${layout.buildDirectory.get()}/compose_compiler"))
}
